# HomeStew Dependencies
# Lightweight stack for PDF processing and LLM integration

# Web framework
# fastapi 0.136.x requires starlette>=0.46.0 with no upper cap, so pip pulls a
# modern Starlette (1.x) transitively — do NOT pin starlette directly.
fastapi==0.136.1
uvicorn[standard]==0.32.0

# Data validation
pydantic==2.9.2
pydantic-settings==2.6.0

# File uploads (FastAPI multipart form support)
python-multipart==0.0.32

# Database
aiosqlite==0.20.0

# PDF processing
pypdf==6.16.2

# HTTP client
requests==2.34.2

# Web search for manual discovery. `ddgs` is the maintained successor of
# duckduckgo-search (same author, MIT licensed) and aggregates several
# engines (bing/brave/duckduckgo/mojeek/startpage/...), so one blocked engine
# no longer kills a search. `primp` is its HTTP client: pin it as a *range*
# because an unpinned transitive bump silently broke manual fetching in
# Sep 2026 (primp 2.x dropped the browser profiles that the old
# duckduckgo_search release hardcoded -> "Invalid impersonate" on every query).
ddgs~=9.16
primp>=2.0.1,<3

# LLM integration
openai==3.7.0

# Secret-at-rest encryption (AES-256-GCM for persisted API keys/tokens)
cryptography==50.0.1
